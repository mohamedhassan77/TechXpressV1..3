// admin.js

// Example: Toggle sidebar (if needed)
$(document).ready(function () {
    $(".toggle-sidebar").on("click", function () {
        $(".admin-sidebar").toggleClass("collapsed");
    });
});
